services:
  globalcorpflags:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: globalcorpflags
    image: suchcodewow/globalcorpflags
    restart: always
    ports:
      - 80:3000
    environment:
      PUBLIC_NEXT_SPLIT_KEY: ""
      NEXT_PUBLIC_TRAFFIC_TYPE: ""
